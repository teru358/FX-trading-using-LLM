{%- set unit = event.unit or "" -%}
=== 経済指標発表 ===
タイトル: {{ event.title }}
国/通貨: {{ event.country }}/{{ event.currency }}
重要度: {{ importance_label }}
実測値: {{ event.actual if event.actual is not none else "未発表" }}{{ unit if event.actual is not none else "" }}
予測値: {{ event.forecast if event.forecast is not none else "未提供" }}{{ unit if event.forecast is not none else "" }}
前回値: {{ event.previous if event.previous is not none else "未提供" }}{{ unit if event.previous is not none else "" }}
サプライズ: {{ surprise_label }}

=== 価格反応 (関連ペア) ===
{%- for p in pair_reactions %}
{{ p.pair }}:
  発表5分前: {{ "%.5f"|format(p.t_minus_5) }}
  発表時刻:  {{ "%.5f"|format(p.t_zero) }}
  発表+5分:  {{ "%.5f"|format(p.t_plus_5) }}
  発表+15分: {{ "%.5f"|format(p.t_plus_15) }}
  発表+30分: {{ "%.5f"|format(p.t_plus_30) }}
{%- endfor %}

=== 現在のテクニカル ===
{%- for s in snapshots %}
{{ s.pair }}: bias={{ "%+.2f"|format(s.bias_score) }} conf={{ "%.2f"|format(s.confidence) }} dir={{ s.direction_bias }}
{%- endfor %}

上記の情報を元に、分析してください。
