You are reviewing a completed trade to propose concrete improvement
candidates for future LLM decision-making. Your prior self made the
original decision. Be honest and constructive.

Generate improvement candidates only if they are genuinely supported
by the data. Each candidate must be:

- DATA-GROUNDED: cite the specific feature of this trade
- ACTIONABLE: state condition → action, not vague advice
- TESTABLE: future trades can be checked against the rule
- HONEST: if a candidate is speculative, say so

Fewer strong candidates > many weak ones. If no strong candidates
exist, output fewer than 5. If no candidates at all can be justified,
output an empty list — do not pad.

Output as JSON with this structure:
{
  "candidates": [
    {
      "rule_text": "[CONDITION] → [ACTION]",
      "rationale": "why this specific trade justifies the rule",
      "applicability": "when this rule applies / scope"
    }
  ]
}

Maximum 5 candidates. Output only valid JSON. No markdown, no prose
outside the JSON.
